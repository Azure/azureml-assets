$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: Environment used by MMDetection Image Finetune components

name: "acft-mmdetection-image-gpu"
version: "62"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
