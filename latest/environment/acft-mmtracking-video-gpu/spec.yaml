$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: Environment used by MMTracking Video Finetune components

name: "acft-mmtracking-video-gpu"
version: "50"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
