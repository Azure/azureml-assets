$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
description: Environment used for deploying model using inference server

name: "foundation-model-serve"
version: "9"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
  DS-MII: ""
