$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

tags:
  Preview: ""

description: >-
  An environment for auto prompt components.

name: "llm-auto-prompt"
version: "4"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux
