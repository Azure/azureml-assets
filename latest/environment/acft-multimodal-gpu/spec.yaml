$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: Environment used by Multimodal classification Finetune components

name: "acft-multimodal-gpu"
version: "2"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
