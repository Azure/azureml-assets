$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  An environment for olive_optimizer cpu components.

name: "olive-optimizer"
version: "6"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux
