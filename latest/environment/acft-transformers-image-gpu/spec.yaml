$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: Environment used by HuggingFace Transformers Image Finetune components

name: "acft-transformers-image-gpu"
version: "2"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
