$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
description: Environment used for deploying model to use deepspeed-mii for inference

name: "foundation-model-inference"
version: "1"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
