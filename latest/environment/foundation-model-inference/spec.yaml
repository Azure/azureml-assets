$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
description: Environment used for deploying model to use Huggingface text-generation
  for inference

name: "foundation-model-inference"
version: "11"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
  HF-Inference: ""
