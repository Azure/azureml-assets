$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  An environment for olive_optimizer_gpu components.

name: "olive-optimizer-gpu"
version: "10"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux
