$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Environment used by Draft Model Training Pipeline

name: "acft-draft-model-training"
version: "1"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
