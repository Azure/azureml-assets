$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Environment used by proxy AOAI components

name: "aoai-data-upload-finetune"
version: "37"

build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
