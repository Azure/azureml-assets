$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  An environment for Large Language Model MIR endpoint components.

name: "llm-dbcopilot-mir"
version: "10"
build:
  path: "context"
  dockerfile_path: "Dockerfile"

os_type: linux

tags:
  Preview: ""
