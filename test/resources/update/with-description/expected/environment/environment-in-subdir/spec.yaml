$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: |-
  ## Test description
  This should be inserted into the spec.yaml file.

name: "environment-in-subdir"
version: "2"

image: "environment-in-parent-dir:2"

os_type: linux
