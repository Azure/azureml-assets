$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Sidecar environment for Windows.

# Should this have an azureml: prefix?
#   See component's environment value of azureml:AzureML-sklearn-1.0-ubuntu20.04-py38-cpu:11
name: "{{asset.name}}"
display_name: "{{asset.name}}"
version: "{{asset.version}}"

image: "{{image.publish.hostname}}/{{image.name}}:{{asset.version}}"

os_type: windows
