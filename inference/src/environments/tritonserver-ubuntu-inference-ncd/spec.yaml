$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  The No-Code deployment environment for the NVIDIA Triton server. 

name: "{{asset.name}}"
display_name: "{{asset.name}}"
version: "{{asset.version}}"

image: "{{image.name}}"

os_type: linux

properties:
  azureml.imagename: "{{image.name}}"

tags:
  Inference: "True"
  No code deploy only: "True"
