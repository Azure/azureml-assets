$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  An MLFlow curated environment for Inference and containing additional python packages.

name: "{{asset.name}}"
display_name: "{{asset.name}}"
version: "{{asset.version}}"

image: "{{image.name}}"

os_type: linux

properties:
  azureml.imagename: "{{image.name}}"

tags:
  Inference: "True"
  Python: "3.7"
  MLFlow: "True"
  OS: Ubuntu18.04
