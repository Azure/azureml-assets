$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  A XGBoost curated environment for Inference, containing scikit-learn and additional python packages.

name: "{{asset.name}}"
display_name: "{{asset.name}}"
version: "{{asset.version}}"

image: "{{image.name}}"

os_type: linux

tags:
  Inference: "True"
  Python: "3.7"
  XGBoost: "0.9"
  Scikit-Learn: "0.23.2"
  OS: Ubuntu18.04
