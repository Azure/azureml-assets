$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  A TensorFlow curated environment for Inference and containing additional python packages.

name: "{{asset.name}}"
display_name: "{{asset.name}}"
version: "{{asset.version}}"

image: "{{image.name}}"

os_type: linux

tags:
  Inference: "True"
  Python: "3.7"
  TensorFlow: "2.4"
  OS: Ubuntu18.04
