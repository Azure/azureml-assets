[pytest]
markers =
    unit: Unit tests
    e2e: End-to-end tests
    smoke: Tests for pull request gates.
    nightly: Tests to be ran nightly.
    longhaul: Tests to be run as a longhaul experiment, usually over the weekend.

filterwarnings =
    ignore::DeprecationWarning