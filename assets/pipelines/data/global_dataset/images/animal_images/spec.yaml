$schema: https://azuremlschemas.azureedge.net/latest/data.schema.json
name: "{{asset.name}}"
version: "{{asset.version}}"
properties:
  azureml.Designer: true
  azureml.Designer.name: Animal Images Dataset
  azureml.Designer.dataTypeId: AnyDirectory
description: This sample dataset is derived from Open Image Dataset and includes 3 animal categories (cat, dog, frog). Each category contains 10 images.
type: uri_folder
path: sample_data/