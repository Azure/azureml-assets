image:
  name: azureml/curated/evaluations-built-in
  os: linux
  context:
    dir: context
    dockerfile: Dockerfile
    template_files:
    - Dockerfile
  publish:               
    location: mcr          
    visibility: unlisted