$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: "{{asset.name}}"
version: "{{asset.version}}"
build:
  path: docker-context
description: Spark environment for running model monitor feature attribution jobs on AzureML compute clusters.
