$schema: https://azuremlschemas.azureedge.net/latest/pipelineJob.schema.json
type: pipeline
display_name: olive-optimizer-built-in-component
description: Use a built-in olive_optimizer component to optimize a bert model.

compute: azureml:cpu-cluster

jobs:
  optimize:
    type: command
    component: file:../../components/cpu/spec.yaml
    inputs:
      config:
        type: uri_file
        path: ./resources/bert.json
      code_path:
        type: uri_folder
        path: ./resources/code
    resources:
      instance_count: 1
