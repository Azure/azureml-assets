$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Environment for TorchForge (torchtitan) training (SFT, GRPO). Includes
  torchtitan with native PyTorch distributed (torchrun) and FSDP2 support
  pre-installed on CUDA 12.6 + PyTorch 2.8.

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{image.context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
  Preview: ""
