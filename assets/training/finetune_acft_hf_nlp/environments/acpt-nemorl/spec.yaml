$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Environment for NeMo-RL-based reinforcement learning training (GRPO, PPO,
  REINFORCE++, SFT). Includes nemo-rl, ray, vllm, megatron-core,
  and nemo-toolkit pre-installed on CUDA 12.6 + PyTorch 2.8.

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{image.context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
  Preview: ""
