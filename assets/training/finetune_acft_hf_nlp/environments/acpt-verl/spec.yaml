$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Environment for VERL-based reinforcement learning training (PPO, GRPO, RLOO,
  REINFORCE++, SFT). Includes verl, ray, vllm, sglang, deepspeed, flash-attn,
  and peft pre-installed on CUDA 12.6 + PyTorch 2.10.

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{image.context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
  Preview: ""
