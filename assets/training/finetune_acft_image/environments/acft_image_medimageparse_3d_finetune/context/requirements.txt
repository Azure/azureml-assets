# MIP-3D finetune env requirements.
# torch / torchvision / torchaudio are installed in the Dockerfile from the
# pytorch cu126 index (so they are not listed here).

# Core scientific stack
numpy==1.26.4
pandas==2.2.2
hydra-core==1.3.2
marshmallow==3.26.2

# Azure ML SDK
azure-ai-ml==1.26.5
azure-identity==1.16.1
azureml-mlflow==1.62.0.post2
azureml-dataprep==5.4.1

# Tracking / logging
mlflow-skinny==3.9.0

# DL training stack
timm==0.9.16
opencv-python-headless==4.11.0.86
# deepspeed: not used by MIP-3D training path. Active Lightning strategy in
# olympus_biomed_parse_3d/configs/trainer/biomedparse_trainer.yaml is
# "ddp_find_unused_parameters_true"; deepspeed strategy + optimizer configs are
# all commented out. Dropping saves ~200MB and avoids JIT-compile risk.
transformers>=5.0.0,<6.0.0
open-clip-torch==2.26.1
sentencepiece==0.2.1
peft>=0.19.1,<0.20.0
ninja==1.11.1.1
kornia==0.7.3
lightning==2.5.5

# Domain / IO
pydicom==2.4.5
SimpleITK==2.4.0
scikit-image==0.24.0
nltk==3.9.4
h5py==3.14.0

# Utilities
python-dotenv==1.2.2
einops==0.8.0
mup==1.0.0
arrow==1.3.0
dotmap==1.3.30
appdirs==1.4.4
# ffmpeg python wrapper dropped: no video codepaths in MIP3D (NIFTI/DICOM only)
# and the OS ffmpeg pkg accounts for 11/12 scan findings.
filelock>=3.20.1

# Build tooling pins (CVE / compatibility)
wheel==0.46.2
setuptools>=82.0.1
protobuf==6.33.5
pillow==12.2.0

# Transitive CVE pins
aiohttp>=3.14.0  # GHSA-hg6j-4rv6-33pg, GHSA-jg22-mg44-37j8
idna>=3.15       # GHSA-65pc-fj4g-8rjx
starlette==1.3.1 # pinned transitive dep of azureml-acft-image-components[mip3d]; fixes GHSA-82w8-qh3p-5jfq
onnx==1.21.0
requests==2.33.0
pytest==9.0.3

# MIP-3D component (built from AzureMlCli, includes olympus_biomed_parse_3d)
azureml-acft-image-components[mip3d]=={{latest-pypi-version}}
