$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: Environment used by MedImageInsight Embedding Generation component

name: acft-medimageinsight-embedding-generator #"{{asset.name}}"
version: 9 #"{{asset.version}}"

build:
  path: ./context #"{{image.context.path}}"
  dockerfile_path: Dockerfile #"{{image.dockerfile.path}}"

os_type: linux

tags:
  Preview: ""
