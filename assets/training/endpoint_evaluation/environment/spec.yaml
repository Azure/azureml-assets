$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  Recommended production-ready RL training environment for large language models (LLMs) wrapping VERL (Volcano Engine Reinforcement Learning) library.
  
name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{image.context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
  PyTorch: "2.8"
  GPU: Cuda12.6
  OS: Ubuntu22.04
  Training: ""
  Preview: ""
  Python: "3.10"
  DeepSpeed: "0.17.4"
