$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
type: command

name: dataset_sampler
display_name: Dataset Sampler
description: Sample a URI_FILE dataset containing a JSONL file.
version: 0.0.1

inputs:
  dataset: 
    type: uri_file
    description: Path to the input .jsonl file from which the data will be sampled.
    optional: False
  sampling_style:
    type: string
    optional: False
    default: head
    description: The sampling method to use. Use `head` to sample from beginning of the file, `tail` to sample from the end of the file and `random` to sample randomly.
    enum:
    - random
    - head
    - tail
  sampling_ratio:
    type: number
    min: 0
    max: 1
    optional: True
    description: Portion of the dataset to be sampled; must be a float in (0,1]; must be null if `n_samples` is specified.
  n_samples:
    type: integer
    optional: True
    description: Absolute number of samples to be taken (alternative to `sampling_ratio`); must be null if `sampling_ratio` is specified.
  random_seed:
    type: integer
    optional: True
    description: Random seed for sampling mode; if not specified, 0 is used. Used only when `sampling_style` is `random`.

outputs:
  output_dataset:
    type: uri_file
    description: Path to the output .jsonl file.

code: ../src
environment: azureml://registries/azureml/environments/model-evaluation/versions/9
command: >-
  python dataset-sampler/main.py 
  --dataset ${{inputs.dataset}} 
  --sampling_style ${{inputs.sampling_style}} 
  $[[--sampling_ratio ${{inputs.sampling_ratio}}]] 
  $[[--n_samples ${{inputs.n_samples}}]] 
  $[[--random_seed ${{inputs.random_seed}}]] 
  --output_dataset ${{outputs.output_dataset}}