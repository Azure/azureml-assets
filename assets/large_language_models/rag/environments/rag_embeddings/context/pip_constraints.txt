# Version constraints used during conda env creation to enforce security patch floors.
# Notes:
# - Constraints do NOT install new packages. They only constrain versions for packages
#   that are already required (directly or transitively) by the environment.
# - Keep this list focused on vulnerable/transitive deps that frequently trigger alerts.

# Python deps from ShaVulnMgmt scan
# (pins are minimum safe versions, not exact pins)
aiohttp>=3.13.4
azure-core>=1.38.0
filelock>=3.20.3
langchain-core>=1.2.22
langchain-text-splitters>=1.1.2
transformers>=5.0.0
marshmallow>=3.26.2
PyNaCl>=1.6.2
urllib3>=2.6.3
Werkzeug>=3.1.5
pyasn1>=0.6.3
wheel>=0.46.2
cryptography>=46.0.7
GitPython>=3.1.48
langsmith>=0.7.31
lxml>=6.1.0
PyJWT>=2.12.0
python-dotenv>=1.2.2
requests>=2.33.0

# Vendored dependency inside setuptools (jaraco.context)
setuptools>=80.10.2
