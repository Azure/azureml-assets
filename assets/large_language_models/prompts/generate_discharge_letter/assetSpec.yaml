$schema: https://azuremlschemas.azureedge.net/latest/asset.prompt.schema.json

type: prompt
name: generate_discharge_letter
version: 0.0.10
display_name: Generate a Discharge Letter
description: Generate a free language discharge letter according to specific doctor notes

tags:  
  industry: healthcare
  task: task-extraction

template:  
  path: ./prompt.yaml

# Specify as top-level 'path' so it will be copied as a data item
# Temporary workaround until we have a proper AssetConfig implemented
path: ./prompt.yaml
