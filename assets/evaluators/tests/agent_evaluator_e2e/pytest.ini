[pytest]
addopts = -n auto
log_cli = true
log_cli_level = INFO
markers =
    requires_connection: test requires external service connection IDs
    self_contained: test only needs project endpoint and model deployment
