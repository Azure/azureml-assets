# Valid tag values that can be applied to evaluation results whose evaluation_type is `text_embeddings`.
task:
  required: True
  allow_multiple: False
  values:
  - bitext_mining
  - classification
  - clustering
  - pair_classification
  - reranking
  - retrieval
  - summarization
  - sts
