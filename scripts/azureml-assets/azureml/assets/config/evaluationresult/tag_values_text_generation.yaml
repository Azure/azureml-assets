# Valid tag values that can be applied to evaluation results whose evaluation_type is `text_generation`.
task:
  required: True
  allow_multiple: False
  values:
  - question-answering
  - text-generation
