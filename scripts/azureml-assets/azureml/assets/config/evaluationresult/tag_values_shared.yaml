# Valid tag values that can be applied to all evaluation results.
evaluation_type:
  required: True
  allow_multiple: False
  values:
    - text_generation
    - text_embeddings
    - text_cost
    - text_performance
    - text_quality
    - vision
