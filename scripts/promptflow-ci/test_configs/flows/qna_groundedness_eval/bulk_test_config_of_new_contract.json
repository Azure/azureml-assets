{
    "mainFlowInputsMapping": {
        "question": "${data.question}"
    },
    "evalFlowInputsMapping": {
        "question": "${data.question}",
        "answer": "{run.outputs.answer}",
        "context": "${data.context}"
    },
    "mainFlowFolderName": "ask_wikipedia"
}