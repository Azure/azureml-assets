$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json

description: >-
  DEPRECATED: Please use tensorflow-2.11-cuda11 or tensorflow-2.12-cuda11 instead. An environment for deep learning with Tensorflow containing the Azure ML SDK and additional python packages.

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{image.context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
  Tensorflow: "2.8"
  GPU: Cuda11
  OS: Ubuntu20.04
  Training: ""
  Preview: ""
  Python: "3.8"
  Deprecated: ""