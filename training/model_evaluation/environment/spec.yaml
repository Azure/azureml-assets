$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
description: >-
  An environment for mlflow based model evaluation tasks. 

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: context/

os_type: linux

tags:
  OS: Ubuntu20.04
  Evaluation: "True"
  MLFlow: "2.1.1"