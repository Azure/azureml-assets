$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
description: Environment used by Model Management components

name: "{{asset.name}}"
version: "{{asset.version}}"

build:
  path: "{{asset.repo.build_context.path}}"
  dockerfile_path: "{{image.dockerfile.path}}"

os_type: linux

tags:
    Preview: ""
